# combining indexOf with get for late-bound field lookup
[main]
output = input.apply("row")

[row]
name    = get(root.headers.indexOf("name"))
species = get(root.headers.indexOf("species"))
type    = root.input_type